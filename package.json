{
  "private": true,
  "name": "purescript-conveyor",
  "scripts": {
    "postinstall": "bower install",
    "clean": "rm -rf output",
    "pulp:build": "pulp build --include example",
    "pulp:version": "pulp version",
    "pulp:publish": "pulp publish",
    "pulp:login": "pulp login",
    "start": "pulp run --include example"
  },
  "dependencies": {
    "bower": "^1.8.4",
    "pulp": "^12.2.0",
    "purescript": "^0.12.0"
  }
}
